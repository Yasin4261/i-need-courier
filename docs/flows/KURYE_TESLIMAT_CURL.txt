═══════════════════════════════════════════════════════════════════
  KURYE TESLİMAT SÜRECİ - CURL KOMUTLARI
═══════════════════════════════════════════════════════════════════
# Token'ları tanımla:
COURIER_TOKEN="eyJhbGciOiJIUzM4NCJ9..."
BUSINESS_TOKEN="eyJhbGciOiJIUzM4NCJ9..."
BASE_URL="http://localhost:8081"
TODAY=$(date +%Y-%m-%d)
───────────────────────────────────────────────────────────────────
ADIM 1: Vardiya Şablonlarını Gör
───────────────────────────────────────────────────────────────────
curl -X GET $BASE_URL/api/v1/courier/shifts/templates \
  -H "Authorization: Bearer $COURIER_TOKEN"
───────────────────────────────────────────────────────────────────
ADIM 2: Vardiya Rezerve Et
───────────────────────────────────────────────────────────────────
curl -X POST $BASE_URL/api/v1/courier/shifts/reserve \
  -H "Authorization: Bearer $COURIER_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"templateId\": 1, \"shiftDate\": \"$TODAY\"}"
# CEVAP: shiftId alacaksın (örn: 123)
───────────────────────────────────────────────────────────────────
ADIM 3: Vardiyaya Giriş Yap (CHECK-IN) ⭐
───────────────────────────────────────────────────────────────────
SHIFT_ID=123
curl -X POST $BASE_URL/api/v1/courier/shifts/$SHIFT_ID/check-in \
  -H "Authorization: Bearer $COURIER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"notes": "Hazırım!"}'
# SONUÇ: Artık on-duty oldun!
───────────────────────────────────────────────────────────────────
ADIM 4: Business Sipariş Oluştur (Arka planda otomatik atanacak)
───────────────────────────────────────────────────────────────────
curl -X POST $BASE_URL/api/v1/business/orders \
  -H "Authorization: Bearer $BUSINESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "endCustomerName": "Test Müşteri",
    "endCustomerPhone": "+905551234567",
    "pickupAddress": "Beşiktaş, İstanbul",
    "deliveryAddress": "Kadıköy, İstanbul",
    "packageDescription": "Test Paketi",
    "packageCount": 1,
    "deliveryFee": 50.00,
    "paymentType": "CASH"
  }'
# CEVAP: orderId alacaksın (örn: 789)
# 3 saniye bekle (otomatik FIFO atama için)
───────────────────────────────────────────────────────────────────
ADIM 5: Bekleyen Atamaları Gör
───────────────────────────────────────────────────────────────────
curl -X GET $BASE_URL/api/v1/courier/assignments/pending \
  -H "Authorization: Bearer $COURIER_TOKEN"
# CEVAP: assignmentId alacaksın (örn: 456)
───────────────────────────────────────────────────────────────────
ADIM 6: Atamayı Kabul Et
───────────────────────────────────────────────────────────────────
ASSIGNMENT_ID=456
curl -X POST $BASE_URL/api/v1/courier/assignments/$ASSIGNMENT_ID/accept \
  -H "Authorization: Bearer $COURIER_TOKEN"
# SONUÇ: Sipariş kabul edildi!
───────────────────────────────────────────────────────────────────
ADIM 7: Paketi Al (PICKUP)
───────────────────────────────────────────────────────────────────
ORDER_ID=789
curl -X POST $BASE_URL/api/v1/courier/orders/$ORDER_ID/pickup \
  -H "Authorization: Bearer $COURIER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"notes": "Paket alındı, yola çıkıyorum"}'
# SONUÇ: Status = PICKED_UP
───────────────────────────────────────────────────────────────────
ADIM 8: Teslimatı Başlat (IN-TRANSIT)
───────────────────────────────────────────────────────────────────
curl -X POST $BASE_URL/api/v1/courier/orders/$ORDER_ID/start-delivery \
  -H "Authorization: Bearer $COURIER_TOKEN"
# SONUÇ: Status = IN_TRANSIT
───────────────────────────────────────────────────────────────────
ADIM 9: Teslim Et (COMPLETE)
───────────────────────────────────────────────────────────────────
curl -X POST $BASE_URL/api/v1/courier/orders/$ORDER_ID/complete \
  -H "Authorization: Bearer $COURIER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "notes": "Müşteriye teslim edildi",
    "collectionAmount": 50.00
  }'
# SONUÇ: Status = DELIVERED ✅
───────────────────────────────────────────────────────────────────
ADIM 10: Vardiyadan Çık (İsteğe bağlı)
───────────────────────────────────────────────────────────────────
curl -X POST $BASE_URL/api/v1/courier/shifts/$SHIFT_ID/check-out \
  -H "Authorization: Bearer $COURIER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"notes": "Vardiya tamamlandı"}'
═══════════════════════════════════════════════════════════════════
✅ ÖZET AKIŞ:
1. Templates → 2. Reserve → 3. Check-in → 4. Business Order
5. Pending Assignments → 6. Accept → 7. Pickup → 8. In-Transit
9. Complete → 10. Check-out
📊 STATUS DEĞİŞİMİ:
PENDING → ASSIGNED → PICKED_UP → IN_TRANSIT → DELIVERED
═══════════════════════════════════════════════════════════════════
